package usuarios;
//import java.util.Vector;

import java.io.FileWriter;
import java.io.IOException;

public class GestorPerUsr {
	
	private UsuarioDom usr;
	
	public GestorPerUsr() {

	}
	
	public void addUser2(UsuarioDom u) {
		try{
			FileWriter esc =new FileWriter("Usuarios.txt");
			esc.write(u.getUser());
			esc.write("\n");
			esc.write(u.getPassword());
			esc.write("\n");
			esc.close();
		}catch(IOException ex){
			
		}
	}
	
	public boolean isUser2(UsuarioDom u){
		/*boolean exist=false;
		for (int i=0; i<2nombre;i=i+2){
			if ((users[i].getUser().equals(u.getUser())) & 
			(users[i].getPassword().equals(u.getPassword()))) {
				exist=true;
			}
		}*/
		return true;
	}
}
